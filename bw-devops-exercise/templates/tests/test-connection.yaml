apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bw-devops-exercise.fullname" . }}-test-connection"
  labels:
    {{- include "bw-devops-exercise.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bw-devops-exercise.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
