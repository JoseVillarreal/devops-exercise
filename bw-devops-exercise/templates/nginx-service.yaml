apiVersion: v1
kind: Service
metadata:
  name: {{ include "bw-devops-exercise.fullname" . }}
  labels:
    {{- include "bw-devops-exercise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bw-devops-exercise.selectorLabels" . | nindent 4 }}
